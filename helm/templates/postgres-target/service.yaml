apiVersion: v1
kind: Service
metadata:
  name: postgres-target
  namespace: {{ .Values.global.namespace }}
  labels:
    app.kubernetes.io/name: postgres-target
    {{- include "cdc.labels" . | nindent 4 }}
spec:
  type: {{ include "cdc.serviceType" .Values.postgresTarget.service.nodePort }}
  selector:
    {{- include "cdc.selectorLabels" "postgres-target" | nindent 4 }}
  ports:
    - name: postgres
      port: {{ .Values.postgresTarget.port }}
      targetPort: {{ .Values.postgresTarget.port }}
      {{- if .Values.postgresTarget.service.nodePort }}
      nodePort: {{ .Values.postgresTarget.service.nodePort }}
      {{- end }}
